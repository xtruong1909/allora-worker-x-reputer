# Sử dụng Docker image TensorFlow GPU đã tích hợp CUDA và cuDNN
FROM tensorflow/tensorflow:latest-gpu

# Thiết lập thư mục làm việc bên trong container
WORKDIR /app

# Cài đặt các phụ thuộc hệ thống cần thiết
RUN apt-get update && apt-get install -y \
    python3-pip \
    python3-dev \
    build-essential \
    git \
    wget \
    && rm -rf /var/lib/apt/lists/*

# Sao chép tệp yêu cầu (nếu có các thư viện Python bổ sung)
COPY requirements.txt .

# Cài đặt các thư viện Python từ requirements.txt
RUN pip3 install --no-cache-dir -r requirements.txt

# Sao chép mã nguồn của ứng dụng vào container
COPY train_models.py app_config.py .

# Đảm bảo TensorFlow có thể sử dụng GPU
ENV TF_FORCE_GPU_ALLOW_GROWTH=true

# Khởi chạy script đào tạo
CMD ["python3", "train_models.py"]
